# ۱۸. تصمیم‌های باز (محدود)

کدساز فقط اگر سند ساکت بود به این فایل نگاه کند.

| کد | موضوع | تصمیم موقت V2 |
|----|--------|----------------|
| OD1 | سکه | پیاده‌سازی به‌صورت optional؛ پیش‌فرض UI خاموش |
| OD2 | PostgreSQL | پشتیبانی connection string؛ تست اصلی SQLite |
| OD3 | فرمول retention | ساده exp بر اساس recency؛ قابل تعویض در domain |
| OD4 | تعداد badge اولیه | ۸ تا ۱۲ نشان در seed |
| OD5 | اشتراک مشاور | لینک توکن‌دار فقط‌خواندنی |

هر چیز دیگر در اسناد اصلی مشخص است — تصمیم جدید نگیر.
