# ۱۴. پرامپت‌های کدساز — ALEMS نسخه ۲

## پرامپت پایه (همیشه اول)

```text
تو مهندس ارشد مسئول پیاده‌سازی ALEMS 2.0 هستی.
فقط طبق اسناد پوشه ALEMS_v2_docs عمل کن.
استک: FastAPI + SQLAlchemy + React + Vite + TS + Tailwind + Framer Motion.
Backend پورت پیش‌فرض 8010، Frontend 5173.
قوانین: TOC-only import مجاز؛ Coverage≠Accuracy≠Volume؛ taught≠learned؛
free time≠capacity؛ Manual Override بر planner غالب است؛ UI نسخه ۱ را ادامه نده.
قبل از کد ساختار را بگو؛ بعد از کد تست و نحوه اجرا را بگو.
پیام خطا فارسی.
```

## فاز ۰ — Foundation + Motion Shell
ساختار پروژه، DB، health، اسکریپت run روی 8010، shell فرانت با Design tokens و Framer Motion، theme dark/light.

## فاز ۱ — Identity & Student & Taught
ثبت‌نام/ورود، پروفایل، check-in، taught topics API+UI onboarding جدید.

## فاز ۲ — Books TOC-only + Tree
Import بدون سوال، block_type، tree UI زیبا، schema endpoint.

## فاز ۳ — Test Engine + Attempts
Session، range/parity، past import، NOT_ENTERED، time tracking، دفترچه خطا.

## فاز ۴ — Review + Learning State
صف، spaced، cluster، weakness، rebuild رویدادمحور.

## فاز ۵ — Planning + Capacity + Today Hub
ظرفیت، generate week، override، today API، manual lock، recovery.

## فاز ۶ — Exam + Analytics + Export
Exam center، scoring، نمودارها، گزارش، PDF.

## فاز ۷ — Rewards + Recommendation + Explain
امتیاز، streak، نشان، پیشنهاد روز، reason فارسی.

## فاز ۸ — UI Polish + Backup + Acceptance
انیمیشن‌ها کامل، focus mode، backup، تست پذیرش، README اجرا.
