"""Security — bcrypt password + JWT access tokens (doc 03 §3.6).

FILLED IN PHASE 1 (Identity & Student):
- hash_password / verify_password (bcrypt)
- create_access_token / decode_access_token (JWT, TTL from Settings)

No external data leaves the core (doc 03 §3.6).
"""
