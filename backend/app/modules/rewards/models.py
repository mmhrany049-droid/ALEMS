"""Motivation (Rewards & Behavior) — SQLAlchemy 2 models.

Points ledger append-only، streak شمسی، badge seed (OD4: ۸ تا ۱۲)، habit advice بعد از ۳۰ روز، procrastination aid (doc 13)

فیلد می‌شود در: فاز ۷
PK: UUID رشته‌ای (doc 05). نرمال‌سازی و ایندکس‌های حیاتی طبق doc 05.
"""
from app.db.base import Base  # noqa: F401 — tables register here in the phase above
