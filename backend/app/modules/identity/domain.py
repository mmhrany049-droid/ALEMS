"""Identity & Session & Permission — pure domain rules, NO I/O (doc 03 §3.1).

ثبت‌نام/ورود با bcrypt، JWT، نقش‌های student/advisor/parent/admin (doc 04, doc 06 §Auth)

قواعد این ماژول باید از توابع domain قابل‌تست بیایند؛ hard-code پراکنده ممنوع (doc 10 §10.4).
فیلد می‌شود در: فاز ۱
"""
