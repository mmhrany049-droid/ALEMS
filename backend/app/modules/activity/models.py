"""Activity & Test Engine — SQLAlchemy 2 models.

Test Record append-only، Test Engine (range/parity/timed)، time tracking، past import با NOT_ENTERED، تیک‌ها، دفترچه خطا (doc 09)

فیلد می‌شود در: فاز ۳
PK: UUID رشته‌ای (doc 05). نرمال‌سازی و ایندکس‌های حیاتی طبق doc 05.
"""
from app.db.base import Base  # noqa: F401 — tables register here in the phase above
