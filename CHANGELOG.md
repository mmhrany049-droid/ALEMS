# Changelog — ALEMS

قالب بر اساس «Keep a Changelog» — نسخه‌گذاری معنایی (SemVer).
نسخه schema پایگاه داده جداگانه در جدول `schema_version` ثبت می‌شود.

## [Unreleased]

### افزوده شد — فاز ۶ (Backup & Polish)
- **پشتیبان خودکار روزانه (قانون ۸.۷)**: حلقه دوره‌ای در سرور (در شروع + هر ۶ ساعت) — حداکثر یک پشتیبان در روز؛ کلید `general.auto_backup_enabled` در تنظیمات (پیش‌فرض فعال) با چک‌باکس در «تنظیمات ← داده‌ها و پشتیبان»
- **README کامل**: دستورات اجرا، معماری، بخش «پشتیبان‌گیری و بازگردانی» (دستی/خودکار/رمزنگاری AES/restore با تأیید)، وضعیت پذیرش
- ۹ تست پشتیبان (`test_backup_auto.py`): ساخت خودکار، سقف روزانه، غیرفعال‌سازی از تنظیمات، رمزنگاری ZIP/AES، رد restore بدون تأیید، سلامت schema پس از restore

### برطرف شد — فاز ۵ (Exam & Analytics)
- 🐛 **باگ تنظیمات**: `set_setting` دیکشنری JSON را درجا mutate می‌کرد و SQLAlchemy تغییر را تشخیص نمی‌داد — از PUT دوم به بعد هر به‌روزرسانی سیاست‌ها (سقف سوال آزمون، ضریب نمره منفی، چرخه مرور و…) عملاً بی‌اثر بود. اکنون با شیء جدید + `flag_modified` همیشه ذخیره می‌شود (+۲ تست رگرسیون)

### افزوده شد — سخت‌سازی فاز ۵ (Exam & Analytics)
- **۱۳ تست API نمره‌دهی** (`test_exam_scoring_api.py`): جریان کامل ایجاد→شروع→ارسال→نتیجه (AT-19/20)، فرمول دقیق `(6−0.33×2)/10×100=53.4` و بدون‌غلط ۶۰ (AT-21)، درصد منفی −33 حفظ می‌شود، همه‌نزده = ۰، تفکیک سختی با الگوی چرخشی (AT-22)، نادیده‌گرفتن سوال بدون سختی، endpoint تحلیل سختی با null برای سختی بدون داده (قانون ۸.۶)، مرز ۲۰۰/۲۰۱ سوال، dedup سوال تکراری
- **گزارش هفتگی در صفحه تحلیل**: کارت «گزارش هفتگی» با جدول روزبه‌روز (تست، درصد کنکوری با قرمز برای منفی، دقیقه مطالعه) + دکمه‌های خروجی PDF و Excel
- فیکسچر آزمون‌ها سقف سوال را صریح ۲۰۰ می‌کند (مقاوم به ترتیب تست‌ها)

### افزوده شد — سخت‌سازی فاز ۴ (Planning)
- **اعتبارسنجی بازه زمانی**: `TimeBlockIn` و `PlanItemIn` اکنون پایان ≤ شروع و قالب نامعتبر HH:MM را با پیام فارسی رد می‌کنند (قبلاً آیتم نامعتبر ذخیره می‌شد)
- **پوشش کامل دقیقه اهداف**: توزیع هدف هفتگی بین روزها با تقسیم سقفی اصلاح شد (قبلاً ۳۰۰ دقیقه → ۴۲×۷=۲۹۴ می‌شد و ۶ دقیقه گم بود؛ اکنون ۳۰۱ ≥ ۳۰۰)
- **تیک «انجام شد» در Today Hub**: آیتم‌های «برنامه امروز» چک‌باکس دارند — علامت‌زدن بلافاصله با `PUT /plans/{date}` ذخیره می‌شود (خط‌خورده نمایش داده می‌شود)
- ۱۵ تست API جدید (`test_planning_api.py`): CRUD اهداف ۳ سطح + فیلتر type، بلوک‌های ۴گانه + رد بازه نامعتبر، تولید هفتگی (شنبه اجباری، عدم تداخل با مدرسه، پوشش دقیقه هدف، هفته خالی مجاز)، ویرایش دستی ماندگار و برنده به تولید (AT-17/18 در سطح API)

### افزوده شد — سخت‌سازی فاز ۳ (Activity & Review)
- **چرخه ۱→۳→۷→۱۴ اصلاح و کامل شد**: مرور اول اکنون دقیقاً +۱ روز پیشنهاد می‌دهد (قبلاً به‌خاطر ترتیب افزایش شمارنده، +۳ می‌داد)؛ سپس +۳، +۷، +۱۴
- **بازگشت خودکار سررسیدها**: در بازسازی صف، آیتم‌های «مرور شده» که تاریخ مرور بعدی‌شان رسیده و چرخه‌شان کامل نشده، دوباره وارد صف می‌شوند (شمارنده مرور حفظ می‌شود)؛ پس از اتمام چرخه (۴ مرور) سوال «جذب‌شده» است و برنمی‌گردد
- **رویداد تیک‌ها**: افزودن/حذف تیک (`important`/`review`/`hard`) صف مرور را خودکار به‌روز می‌کند (Event Bus → `question_marks.changed`)
- **«بعداً» (سند 07 §7.4)**: endpoint `POST /reviews/{id}/postpone` + دکمه در صف مرور و Today Hub — تاریخ آیتم جابه‌جا می‌شود و pending می‌ماند
- آمار بازسازی صف شامل `reentered` شد
- ۱۳ تست سرویس جدید: زنجیره کامل ۴ مرور، بازگشت سررسید با حفظ شمارنده، ورود/خروج با تیک، postpone (معتبر/نامعتبر/done)، دفترچه خطا

### افزوده شد — سخت‌سازی فاز ۲ (Knowledge Base)
- **Schema رسمی فایل کتاب** (`backend/samples/book.schema.json` — JSON Schema draft-07) + endpoint `GET /resources/import-book/schema` که schema و نمونه را برمی‌گرداند
- **پشتیبانی زیرمبحث در import**: کلید اختیاری `subtopics` زیر هر مبحث (فقط یک سطح — مطابق درخت سند: درس ← فصل ← مبحث ← زیرمبحث)؛ زیرمبحث‌ها با `parent_id` ذخیره می‌شوند
- **کتاب تکراری** اکنون 409 Conflict با `details.duplicate` برمی‌گردد (قبل: 422)
- **تطبیق دقیق درس**: کلیدهای اختیاری `field`/`grade` در فایل کتاب → تطبیق نام+رشته+پایه
- **درخت منبع**: `GET /resources/{id}/tree` — فصل ← مبحث ← زیرمبحث با تعداد سوال هر گره (محدود به مبحث‌های همان کتاب)
- **Frontend**: مرورگر «درخت دروس» در تب جدید صفحه مطالعه — فیلتر خودکار بر اساس پایه/رشته پروفایل، پیمایش درس→فصل→مبحث→زیرمبحث، فهرست کتاب‌های هر درس و مشاهده سوالات با طبقه‌بندی (AT-09)
- ۱۴ تست جدید فاز ۲ (زیرمبحث، schema، تکراری 409، تطبیق دقیق، طبقه‌بندی، پیمایش درخت)

### افزوده شد — سخت‌سازی فاز ۱ (Identity & Profile)
- نخستین کاربر هر پایگاه داده نقش «admin» می‌گیرد (سند 01 §1.5 — مدیر سیستم)؛ بقیه student
- `full_name` هنگام ثبت‌نام، پروفایل اولیه می‌سازد (ناقص تا تکمیل پایه/رشته)
- صفحه **آغازکار (Onboarding)** در Frontend: کاربر بدون پروفایل کامل پیش از بخش‌های اصلی به تکمیل پروفایل هدایت می‌شود (سند 08 §8.6)
- ۲۴ تست واحد و جریان کامل جدید: ثبت‌نام (اعتبارسنجی‌ها، عدم افشای هش)، ورود (توکن، ضد user enumeration)، خروج واقعی (لغو نشست)، Permission (admin/student)، جداسازی چندپروفایل، و مسیر کامل «ثبت‌نام → ورود → تکمیل پروفایل → ثبت انرژی روزانه»

## [1.0.0] — ۱۴۰۵/۰۶/۲۸ (2026-09-19)

### افزوده شد — نسخه ۱ کامل (MVP + Core)

**فاز ۰ — Foundation**
- ساختار Modular Monolith با ۱۱ ماژول دارای مرز واضح
- Core System: تنظیمات مرکزی (`core/config.py`) + Event Bus سبک (`core/events.py`)
- Database Management: SQLAlchemy 2.0 + SQLite (WAL, foreign_keys) + Alembic
- File Management: مسیرهای استاندارد `data/ backups/ exports/ imports/` + خواندن/نوشتن JSON (`core/files.py`)
- Version Management: ثبت نسخه برنامه و schema در جدول `schema_version` + `/health` (`core/versioning.py`)
- Endpoint سلامت `GET /health` (AT-01)
- سیستم لاگ و مدیریت خطای پایه — پاکت پاسخ `{success, data, error, meta}` با پیام‌های فارسی
- `pyproject.toml` + `requirements.txt` + `package.json` آماده اجرا؛ اجرای کامل با یک دستور (`scripts/run.sh`)

**فاز ۱ — Identity & Profile** (AT-02 تا AT-05)
- ثبت‌نام/ورود/خروج با bcrypt + JWT، مجوز student/admin
- پروفایل دانش‌آموز (پایه/رشته/هدف) + وضعیت روزانه (انرژی ۱-۵، حال، شرایط)

**فاز ۲ — Knowledge Base** (AT-06 تا AT-09)
- درخت دروس رشته→پایه→درس→فصل→مبحث + ۶۷ درس پیش‌فرض
- وارد کردن کتاب تست JSON با اعتبارسنجی فارسی + تشخیص کتاب تکراری

**فاز ۳ — Activity & Review** (AT-10 تا AT-14)
- ثبت دسته‌ای تست + تیک‌های چندگانه + دفترچه خطا (۴ نوع اشتباه)
- صف مرور خودکار (غلط + نزده اختیاری + تیک مرور/مهم/سخت) با چرخه ۱-۳-۷-۱۴

**فاز ۴ — Planning** (AT-15 تا AT-18)
- اهداف بلندمدت/ماهانه/هفتگی + بلوک‌های زمانی ثابت
- تولید خودکار برنامه هفتگی (شنبه تا جمعه) + ویرایش دستی + Today Hub

**فاز ۵ — Exam & Analytics** (AT-19 تا AT-26)
- آزمون آزمایشی/امتحان با تایمر؛ درصد کنکوری `(C − ۰.۳۳W)/T×۱۰۰` و بدون غلط
- تحلیل سختی، تحلیل به تفکیک درس/مبحث، آمار نوع اشتباهات
- گزارش روزانه/هفتگی/ماهانه (جلالی) + خروجی PDF فارسی (وزیرمتن+RTL)، Excel، JSON

**فاز ۶ — Backup & Polish** (AT-27 تا AT-31)
- Backup consistent (SQLite backup API) + رمزنگاری AES اختیاری + Restore دومرحله‌ای
- وب‌اپ کامل فارسی RTL با فونت وزیرمتن، تقویم شمسی، Empty Stateها

### تست‌ها
- ۸۷ تست واحد و پذیرش (دامنه + AT-01 تا AT-27)

## [Unreleased]
- پیش‌بینی نسخه ۲: داشبورد مشاور/والد، همگام‌سازی چنددستگاهی، Spaced Repetition پیشرفته
